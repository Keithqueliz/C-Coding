#include <stdio.h>
#include <math.h>

int main()
{   
    double A = sqrt(9);
    double B = power(2,4);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(3.9);
    double F = fabs(-100);
    double G = log(3);
    

    return 0;
}